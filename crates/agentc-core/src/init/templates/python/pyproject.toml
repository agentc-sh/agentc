[project]
name = "{{ name_snake }}"
version = "0.1.0"
description = ""
readme = "README.md"
authors = []
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
