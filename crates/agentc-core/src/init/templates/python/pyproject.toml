[project]
name = "{{ name_snake }}"
version = "0.1.0"
description = ""
readme = "README.md"
authors = []
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "agentc-runtime @ git+https://github.com/agentc-sh/agentc@{{ agentc_version }}#subdirectory=packages/python/runtime",
    "pytest>=9.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pyright]
reportMissingModuleSource = false
